{
  "name": "@paxol/scripts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf .next .turbo node_modules",
    "migrate": "pnpm with-env ts-node migrate.ts",
    "docker:build": "pnpm with-env ts-node dockerBuild.ts",
    "type-check": "tsc --noEmit",
    "with-env": "dotenv -e ../.env --"
  },
  "dependencies": {
    "@paxol/db": "*"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "dotenv-cli": "^7.0.0",
    "eslint": "^8.32.0",
    "eslint-config-next": "^13.1.6",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  }
}
