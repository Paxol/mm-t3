{
  "name": "@paxol/mm-faker",
  "version": "0.1.0",
  "scripts": {
    "clean": "rm -rf .next .turbo node_modules",
    "start": "pnpm with-env ts-node index.ts",
    "type-check": "tsc --noEmit",
    "with-env": "dotenv -e ../.env --"
  },
  "dependencies": {
    "@inquirer/confirm": "^5.0.2",
    "@inquirer/select": "^4.0.2",
    "@paxol/api": "*",
    "@paxol/db": "*",
    "zod-schema-faker": "^1.0.5"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "dotenv-cli": "^7.0.0",
    "eslint": "^8.32.0",
    "eslint-config-next": "^13.1.6",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  }
}
